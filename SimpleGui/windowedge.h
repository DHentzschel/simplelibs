#ifndef WINDOWEDGE_H
#define WINDOWEDGE_H

#include <types.h>

enum class WindowEdge : byte {
    Left,
    Right,
    Top,
    TopLeft,
    TopRight,
    Bottom,
    BottomLeft,
    BottomRight
};

#endif // WINDOWEDGE_H
